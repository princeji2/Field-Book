node_modules

# Local secrets — MUST NOT be committed.
.env
.env.local
.env.*.local
# Build output — regenerated by `vite build`, not checked in.
dist
.vercel
.env*

# Supabase CLI local cache/state — not part of the project.
supabase/.temp
